1. AR3DV 安装4.5.5版本的opencv。 修改CMakeList然后cd + cmake ..
2. PointnetSeg已经添加了captra的util，测试看看dataloader的数据是否正确
3. NorProject 添加removeborder效果如何，如果还是集中于一个平面，说明loss的计算依然有问题（也有可能是loss权重的问题？）
		我认为大概率是loss的问题，不知道为什么总是会集中于一个平面上，点都分布在一个平面上会让某一个loss更小吗？目前来看占比重大的loss是对应corr_loss

		不，造成这种现象的原因是找到了错误的对应点，》》观测点云pts1是否应该通过coord与pts2构建对应关系？？？？《《
		目前构建对应关系是将pts1通过gt位姿变换变换到2的坐标系下作为gt，同时网络根据预测的对应关系在2下用pts2的点加权得到映射点云pts1in2

		但目前的问题是，pts1in2在初始时并不能正确的映射，因为一开始assingmatrix甚至在所有点上的权重都是一样的，所以一开始初始化pts1in2应该在pts2的平均值点mean，
		如何去修正？
		当前，mean和在pts2上的对应点作差，为了缩小loss，将权重向周围分配

		########### 可是为什么会出现变成一条直线的趋势？等明天跑完remove_border看看。

		# 也可以挑一个中间结果，取pts1和pts2计算一下loss，看看具体是怎么样的，assign_matrix的映射又是怎么样的。我现在怀疑是多个点都映射到一个点上了。

		########## 可视化assignmatirx的代码应该提上日程了。


、

		原本的目的是希望，通过A局部特征+A全局特征+B全局特征 得到 A到B的映射。但这样下去的话似乎效果不行？现在看来 似乎A局部XB局部.T 更合适？如果有余力可以做这个对照实验。


4.